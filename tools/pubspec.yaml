name: figma_export_tool
description: Connect to running Flutter app via VM Service and export to Figma JSON
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  vm_service: ^14.0.0
  image: ^4.0.0
